# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: GoogleapisClientFlutterBindings
description: |
  Bindings for `src/googleapis_client_flutter.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/googleapis_client_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/googleapis_client_flutter.h'
  include-directives:
    - 'src/googleapis_client_flutter.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
